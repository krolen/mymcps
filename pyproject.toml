[project]
name = "mcp-servers"
version = "0.1.0"
description = "MCP Servers collection including SearXNG search"
requires-python = ">=3.12"
dependencies = [
    "fastmcp>=3.2.0",
    "httpx>=0.27.0",
    "requests>=2.33.1",
    "crawl4ai==0.8.6",
]
